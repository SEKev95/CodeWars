function sumStr(a,b) {
  return String(Number(a)+Number(b));
}
//P- giving two numbers as a string
//r- the sum of the two numbers
//e- ("4", "5") the sum should be "9"
//p- need to keep it as a string, but to also add the numbers as regular intergers