// Beginner - Lost Without a Map
function maps(x){
    return x.map(n => n * 2)
   }
   
   //p given an array
   //r the nummbers in the array doubled
   //e [1, 2, 3] --> [2, 4, 6]
   //p told us to use the map method