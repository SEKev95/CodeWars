// Small enough? - Beginner

function smallEnough(a, limit){
    return a.every(v=>v<=limit)
   }
   
   //given an array and limit value
   //r the value of the array are below or equal to the limit value , return true. else, return false
   //e [1,2,3] value = 6 limit 3 returns false 