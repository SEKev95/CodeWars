// Find Maximum and Minimum Values of a List

const min = (list) => Math.min(...list);
const max = (list) => Math.max(...list);

//P make two functions
//R array/vector of integers list as input and outputs, respectively,
//E max([4,6,2,1,9,63,-134,566]) returns 566
//  min([-52, 56, 30, 29, -54, 0, -110]) returns -110
//  max([5]) returns 5
//P math.min math.max
